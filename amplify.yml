version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build needed (pre-built Flutter web)"
    build:
      commands:
        - echo "Skipping build phase"
  artifacts:
    baseDirectory: build/web
    files:
      - '**/*'
  cache:
    paths: []
