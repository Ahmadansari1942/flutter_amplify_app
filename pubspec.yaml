name: flutter_amplify_app
description: AhmadBlog Style Flutter Web UI
publish_to: 'none'
version: 2.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
